# Generated by makegen....

TARGET = server
OBJECTS = config_server.o ../commun/bcast_config.o server.o

CC = gcc
CFLAGS =   $(INCDIR) -O3

LIBS = -lm -lpthread
LIBDIR =  
INCDIR = -I./

INSTALLDIR = 

all:clean $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET) $(OBJECTS) $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS)

