# Generated by makegen....

TARGET = client
OBJECTS = config_client.o client.o ../commun/bcast_config.o

CC = gcc
CFLAGS =   $(INCDIR) -O3

LIBS = -lm -lpthread
LIBDIR =  
INCDIR = -I./

INSTALLDIR = 

all:clean $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET) $(OBJECTS) $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS)

